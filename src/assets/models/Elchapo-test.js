/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef, useState } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'
import * as THREE from "three";
import { useFrame } from '@react-three/fiber';

export default function Model({ ...props }) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('./models/elchapo-test.gltf')
  const { ref, names, actions, clips } = useAnimations(animations)
  console.log('action :>> ', actions);
  useEffect(() => {
    console.log('action2', actions)
    let test = actions['Armature|mixamo.com|Layer0']
    if (test) {
      // test.clampWhenFinished = true
      // test.setEnding(1)
      test.loop = THREE.LoopOnce;
      test.clampWhenFinished = true;
      // test.enable = true;

      setTimeout(() => {
        // test.paused =true
        test.play()

      }, 2000);
    }
  }, [])

  const [mixer] = useState(() => new THREE.AnimationMixer())
  useFrame((state, delta) => mixer.update(delta))
  return (
    <group ref={ref} {...props} dispose={null}>
      <group name="Scene">
        <group name="Armature" rotation={[Math.PI / 2, 0, 0]} scale={0.01}>
          <primitive object={nodes.mixamorigHips} />
          <group name="body">
            <skinnedMesh name="Mesh001" geometry={nodes.Mesh001.geometry} material={materials['eyes_MAT.002']} skeleton={nodes.Mesh001.skeleton} />
            <skinnedMesh name="Mesh001_1" geometry={nodes.Mesh001_1.geometry} material={materials['body_MAT.002']} skeleton={nodes.Mesh001_1.skeleton} />
            <skinnedMesh name="Mesh001_2" geometry={nodes.Mesh001_2.geometry} material={materials['head_MAT.002']} skeleton={nodes.Mesh001_2.skeleton} />
          </group>
          <skinnedMesh name="cloth" geometry={nodes.cloth.geometry} material={materials['cloth_MAT.002']} skeleton={nodes.cloth.skeleton} />
          <skinnedMesh name="hair" geometry={nodes.hair.geometry} material={materials['hair_MAT.002']} skeleton={nodes.hair.skeleton} />
          <skinnedMesh name="tooth" geometry={nodes.tooth.geometry} material={materials['tooth_MAT.002']} skeleton={nodes.tooth.skeleton} />
          <skinnedMesh name="trousers" geometry={nodes.trousers.geometry} material={materials['pants_MAT.002']} skeleton={nodes.trousers.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/elchapo-test.gltf')
