/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useEffect, useRef } from 'react'
import { useGLTF, useAnimations } from '@react-three/drei'

export default function Model({ ...props }) {
  const group = useRef()
  const { nodes, materials, animations } = useGLTF('./models/foxNew2.gltf')
  const { actions } = useAnimations(animations, group)

  // materials.metalness = 10
  useEffect(() => {
    }, [])
  
  return (
    <group  scale={0.025} position={[0,0.001,0]} rotation={[0 , 2 , 0]} ref={group} {...props} dispose={null}>
      <group >
        <group  name="root">
          <primitive  object={nodes._rootJoint} />
          <skinnedMesh castShadow name="fox" geometry={nodes.fox.geometry} material={materials.fox_material} skeleton={nodes.fox.skeleton} />
        </group>
      </group>
    </group>
  )
}

useGLTF.preload('/foxNew.gltf')
